/**
 * Shared input/select class string.
 * Replaces the scattered `outline-none` pattern with proper focus-visible ring.
 */
export const inputCls =
  "rounded-lg px-3 py-1.5 text-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent";
